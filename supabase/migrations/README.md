# Database migrations

These files are the record of the schema changes applied to the Supabase
project. They were applied through the Supabase migration API and are kept here
so the database history is reviewable alongside the app code.
